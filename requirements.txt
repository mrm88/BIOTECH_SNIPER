# Biotech Sniper — pinned dependencies.
#
# All entries MUST be fully pinned via ``==``. No unpinned packages, no
# version ranges. The mission validator (VAL-M1-016) greps for any line
# without ``==`` and fails the build if one is found.
#
# Python 3.10 (VPS) and 3.12 (local) are both supported.

# --- HTTP / parsing ---------------------------------------------------------
requests==2.32.3
# Transitive deps of `requests` pinned explicitly to silence
# `RequestsDependencyWarning`. requests 2.32.x runs a per-import
# `check_compatibility` that asserts:
#   - urllib3 >= 1.21.1 (no upper bound today, but pinning insulates us)
#   - chardet in [3.0.2, 6.0.0)        <-- chardet 7.x trips this
#   - charset_normalizer in [2.0.0, 4.0.0)
# See f-misc-12-vps-urllib3-chardet-drift in library/environment.md.
urllib3==2.2.3
chardet==5.2.0
charset-normalizer==3.4.0
feedparser==6.0.11
beautifulsoup4==4.12.3

# --- Market data / reporting ------------------------------------------------
# Options-chain data is sourced from alpaca-py via
# biotech_sniper.options_chains.pull_options.
openpyxl==3.1.5
reportlab==4.2.5

# --- Trading ---------------------------------------------------------------
alpaca-py==0.34.0

# --- Configuration ----------------------------------------------------------
python-dotenv==1.0.1

# --- Testing ----------------------------------------------------------------
pytest==8.3.4
pytest-cov==6.0.0
vcrpy==6.0.2

# --- LLM clients (M2) -------------------------------------------------------
anthropic==0.42.0
google-genai==0.4.0

# --- ML ranker / feature store (M5) -----------------------------------------
lightgbm==4.5.0
pandas==2.2.3
pyarrow==18.1.0
